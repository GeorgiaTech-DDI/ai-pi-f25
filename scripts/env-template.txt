# Environment Variables Template for AI PI Admin Portal
# Copy this content to .env and fill in the actual values

# Firebase Configuration (for admin authentication)
# Get these values from your Firebase project console > Project settings > Web apps
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_firebase_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id

# Optional: Set to production to enable secure cookies over HTTPS only
NODE_ENV=development

# Instructions:
# 1. Create a Firebase project at https://console.firebase.google.com/
# 2. Enable Authentication > Email/Password sign-in method
# 3. Create an admin user in the Authentication console
# 4. Get your web app config from Project settings > General > Your apps
# 5. Replace the placeholder values above with your actual Firebase config
# 6. Never commit .env to version control!
