# Environment Variables Template for AI PI Admin Portal
# Copy this content to .env.local and fill in the actual values

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here

# JWT Secret Key - MUST be a strong random string
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_very_strong_random_secret_key_here

# Optional: Set to production to enable secure cookies over HTTPS only
NODE_ENV=development

# Instructions:
# 1. Generate a strong password hash using: node scripts/hash-password.js "your_secure_password"
# 2. Generate a JWT secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 3. Replace the placeholder values above with your actual values
# 4. Never commit .env.local to version control!
