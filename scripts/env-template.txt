# Environment Variables Template for AI PI Admin Portal
# Copy this content to .env.local and fill in the actual values

# Azure AD (Microsoft Entra ID) Configuration for Admin Authentication
# Only @gatech.edu email addresses are allowed to access the admin portal
# Get these values from Azure portal > Microsoft Entra ID > App registrations
NEXT_PUBLIC_AZURE_AD_TENANT_ID=your_tenant_id
NEXT_PUBLIC_AZURE_AD_CLIENT_ID=your_app_client_id
NEXT_PUBLIC_AZURE_AD_REDIRECT_URI=http://localhost:3000
NEXT_PUBLIC_AZURE_AD_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# Optional: Set to production to enable secure cookies over HTTPS only
NODE_ENV=development

# Instructions:
# 1. In Azure portal, register a new app (Single-page application).
# 2. Add redirect URIs for local and production (must match above envs).
# 3. Under Authentication, enable ID tokens.
# 4. Under API permissions, add "User.Read" (Microsoft Graph).
# 5. Optionally restrict to your tenant by using the tenant ID in the authority.
# 6. Replace the placeholders above with actual values.
# 7. Never commit .env.local to version control!

# Security Features Enabled:
# ✅ Only @gatech.edu email addresses are allowed to authenticate
# ✅ Tenant restriction (only Georgia Tech Azure AD tenant)
# ✅ Minimal OAuth scopes (User.Read only)
# ✅ Session timeout after 30 minutes of inactivity
# ✅ Automatic token refresh (5 minutes before expiry)
# ✅ Enhanced security headers (CSP, HSTS, etc.)
# ✅ Activity tracking for session management
# ✅ Automatic logout for invalid email domains
# ✅ Secure token storage in localStorage (encrypted by MSAL)

# Additional Security Recommendations:
# 1. Use HTTPS in production (required for HSTS)
# 2. Regularly rotate your Azure AD client secret
# 3. Monitor authentication logs in Azure AD
# 4. Consider implementing IP restrictions if needed
# 5. Enable MFA for your Azure AD tenant
